<h1>Cadastrar Detalhe</h1>

<br>
<fiedset>
  <%= form_for(@imovel_detalhes, :html => { :class => :form }) do |f| -%>
  
  <%= f.label 'Nome', :class => "control-label" %>
  <%= f.text_field :nome, :class => "input-xxlarge", :onkeyup => "toUppercase(this);" %>

<br>  
  <div class="form-actions-white">
    <!-- <%= f.submit %> -->
    <% if @imovel_detalhes.id.nil? %>
    <button class="btn btn-primary" type="submit">Cadastrar</button>
    <% else %>
    <button class="btn btn-info" type="submit">Alterar</button>
    <% end %>
    ou 
    <%= link_to "Cancelar", imovel_detalhes_path, :confirm => "Deseja cancelar a operação? Todos os dados serão perdidos." %>
  </div>
<% end %>
</fiedset>