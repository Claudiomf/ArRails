<h1>Características Cadastradas</h1>

<div class="well-small">
  <dl>
    <dd>
      Nesta página, o administrador pode alterar, remover e visualizar detalhes de imoveis cadastrados.
    </dd>
  </dl>
</div>
<br>
<div class="well well-small" style="text-align: center;">
  <i class="icon-pencil"></i> Alterar | <i class="icon-trash"></i> Remover |<%= link_to(new_caracteristica_imovel_path, :class => "btn btn-link link-icon") do %><i class="icon-plus-sign"></i> Cadastrar Característica <% end %>
</div>


<table class="table table-bordered">
  <thead>
    <tr>
      <th class="rotulo">Nome</th>
      <th class="rotulo" style="text-align: center; width: 20%">Data de Cadastro</th>
      <th class="rotulo" style="text-align: center; width: 20%"></th>
    </tr>    
  </thead>
  <tbody>
    <% if !@caracteristica_imovels.empty? %>
      <% @caracteristica_imovels.each do |caracteristica| %>
        <tr>
          <td><%= caracteristica.descricao %></td>
          <td style="text-align: center;"><%= l(caracteristica.created_at, :format => :sample) %></td>
          <td style="text-align: center; width: 20%">
              <%= link_to(edit_caracteristica_imovel_path(caracteristica), :class => "link-icon", :title => "Alterar") do %>
               <i class="icon-pencil icon-white"></i>
              <% end %> |
            <%= link_to(caracteristica, :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Tem certeza que deseja remover este imóvel?")}", :class => "link-icon", :title => "Remover" ) do %>
             <i class="icon-trash icon-white"></i>
            <% end %>
          </td>
        </tr>
      <% end %>
    <% else %>
    <tr>
        <td colspan="3" style="text-align: center">
          <font class="text-error" >Não há características cadastradas.</font>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>